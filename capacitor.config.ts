import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.khaata.app',
  appName: 'Khaata',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
